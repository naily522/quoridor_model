# =============================================================================
# Python 依赖清单 — requirements.txt
#
# 安装方法:
#   pip install -r rl/requirements.txt
#
# 如果使用 PyTorch，建议根据你的 CUDA 版本从 pytorch.org 安装，
# 此处列为参考。
# =============================================================================

torch>=2.0.0
numpy>=1.24
